{
 "name": "root",
 "private": true,
 "workspaces": [
  "packages/*"
 ],
 "scripts": {
  "clean": "lerna clean -y",
  "watch": "lerna run watch --stream",
  "build": "lerna run build --concurrency=3",
  "dev": "lerna run dev:start --stream"
 },
 "devDependencies": {
  "@types/node": "^24.9.1",
  "lerna": "^9.0.0",
  "typescript": "^5.9.3"
 }
}
